# 星めぐり航海日誌 — 仕様

## 0. ドキュメント情報
- 対象ゲーム: 250-hoshimeguri-kokai-nisshi
- 作成日: 2026-09-20
- 更新日: 2026-09-23
- ステータス: 公開準備
- 参照ファイル: index.html / fc-ios-controller.js / fc-ios-controller.css / three.min.js

## 1. ゲーム概要
- ジャンル: 3D航行・探索ログ
- 一言説明: 太陽系を探索して科学素材を集め、船体を強化しながら外惑星へ到達する。
- 想定プレイ時間: 15〜40分
- 対象端末: iPhone Safari / GitHub Pages

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 対応画面幅: 320px〜430px
- 実装方式: 静的 HTML / CSS / JS。Three.js r128 はローカル同梱

### 任意
- PC: キーボード（WASD・矢印・E記録・P撮影・L着陸・Shift加速・Enterポーズ）

## 3. 操作 / 設定UI

- 画面構成: 上75% `#game-stage` / 下25% `#control-deck`（ハーネス契約）
- 入力: Pointer Events + `setPointerCapture`
- 十字: 航行は機首/旋回。地表は前後移動＋左右で視点旋回（平行移動と重ねない）
- 航行中の画面ドラッグ: 旋回（チェイスカメラが視点を上書きするため）
- A: 文脈行動。周回では観測 / 着陸、地表では発見物が近ければ記録、なければジャンプ
- B: 撮影
- 「空を見る」（キー Q ／ 地表の行動ボタン）: 空にしかない記録が残っているとき、または見上げている最中に表示。押すと仰角を 0.95rad へ自動で振り切り、旋回だけで空の的を画面へ入れられる（2026-09-23 追加。ドラッグは下方向で見上げる操作で、iPhone のコントローラには仰角入力が無く、空の記録が取得不能だった）
  - 解除は「視点を戻す」／画面を縦にドラッグ。解除すると地表の既定 0.13rad へ戻る。撮影で記録が成立した瞬間にも自動で戻す（ボタンだけ消えて見上げたまま取り残される状態を防ぐ）
- SELECT: 戻る。地表では船へ即帰還、航行・周回では星図へ戻る
- START: ポーズ
- 操作盤の小ラベルは状態に応じて更新し、現在押すと何が起きるかを表示する
- HUD・星図の出発・ポーズは pointerdown + setPointerCapture。星図の行き先リストはスクロールを奪わない `onPtrTap`（capture なし）
- ミュート: ポーズ内「音の切り替え」と `m` キー。キー `tg.250.mute`
- 航海記録: `hoshimeguri.v1`（既存キー維持）
- 開始: タイトル「出港する」の pointerdown で AudioContext unlock

## 4. コアループ
- タイトル → 星図で解放済みの行き先を選ぶ → 航行 → 周回観測 → 着陸探索 → 科学素材を回収 → 船体強化 → 遠方航路を解放
- 観測・地表発見1件につき、その天体固有の科学素材を1個回収する。既存セーブは記録キーから素材数を再計算する
- 船体強化は自動。**素材＝観測・地表発見の記録1件につき1個**。星図ヘッダと詳細パネルに「あと◯件で HULL ◯（◯◯の航路が開く）」を常時表示し、強化条件を画面だけで分かるようにする（2026-09-22）
- 船体は段階ごとに**形が変わる**（`buildShip(tier)`）。I=双発の基本形／II=放熱フィン＋補助エンジン2基（計4基）／III=通信皿・カナード・与圧タンク／IV=前面衝撃盾リング＋中央主機（計5基）。外装色も I 白銀 → IV 金へ推移する
- 素材数と航路は次の4段階
  - HULL I（0個）: 太陽・水星・金星・地球・月
  - HULL II（6個）: 火星を解放
  - HULL III（14個）: 木星・土星を解放
  - HULL IV（24個）: 天王星・海王星を解放
- ロック天体は星図で必要船体と不足素材数を表示し、出発操作を無効化する
- 星図は **2列の行き先ボタン**（幅640px以上、または高さ520px以下は5列）。10天体が名前付きで見える。短い画面ではリストだけが縦スクロールし、詳細と「出発」は下に固定する
- 動線: 行き先をタップして選ぶ → 説明を読む → 「◯◯へ出発」。横スクロールの点列は使わない（iPhone SEで潰れて消えていた）
- 達成率は到達・観測・着陸・発見の記録数
- 太陽系全82項目を埋めることがクリア条件

## 4-2. エンディング

- 全82項目達成で `solar:complete` を保存し、専用エンディングへ遷移する
- 静かな航海日誌の画面を、赤・シアン・黒、走査線、グリッチ、斜線で破壊するパンキッシュな演出
- 内容は「太陽系の先はやはり広かった」。外から振り返ると太陽系さえ一粒の航路標識に見えるほど、広大・壮大・雄大な宇宙を描く
- 未知は脅威として閉じず、「まだ名前のない星」「説明できない何か」「十一番目の座標」で、畏敬と次の航海を残す
- エンディングから航海日誌と星図へ戻れる。完了済みセーブでは再起動時にもエンディングを再表示する

## 5. 画面 / 状態遷移
- `boot` → `attract`（タイトル）→ `map` → `cruise` → `orbit` → `descend` → `surface` → `ending`（全項目達成時）
- ポーズは航行中・地表で明示停止
- 日誌はどの画面からも開閉できる

## 5-2. 天体の見え方（2026-09-21 改修）

- 惑星テクスチャは**球面座標で3Dノイズを引いて焼く**（UVで引くと極で模様が潰れ赤道で伸びる）。解像度 768×384、天体ごとに1回だけ生成してキャッシュ
- 種別ごとの作り分け
  - `rock`（月・水星・火星・フォボス）: 大地形＋尾根＋クレーター（椀・縁・中央丘・放出物）。月は海（マリア）、火星は明暗のアルベド模様と極冠、水星は高密度クレーター
  - `ice`（エウロパ等）: なめらかなノイズの**等高線**をリネア（氷殻の裂け目）として拾う
  - `earth`: ひねった大陸＋緯度と標高で植生・砂漠・雪、極冠、雲を焼き込む。海は約7割
  - `band`（木星〜海王星）: 緯度の帯をノイズで歪めたゾーナルな流れ＋渦（大赤斑・白斑・大暗斑）
  - `cloud`（金星・タイタン）: 地表を見せない霞。金星はダークY
  - `star`（太陽）: 粒状斑（粒と暗いレーン）・超粒状斑・黒点群（暗部＋半暗部）
- 凹凸は **bumpMap**（岩と氷のみ）。大地形ではなく細部だけを焼き、クレーターが光の当たり方で立つようにする
- 材質は **MeshPhongMaterial**（Standardの物理BRDFは1画素あたりが重い）。アルベドは `sRGBEncoding`
- 大気は**フレネルの縁光り**（`ShaderMaterial`）。大気のある星（地球・ガス惑星・金星/タイタン・火星）だけに付け、月や水星には付けない
- 太陽は**周縁減光**付きのシェーダー（中央が明るく縁が暗い）

## 5-3. 地表描画・接地・重力（2026-09-22 改修）

- 地表は高さ、斜度、微細ノイズを頂点色へ反映し、反復生成した細粒テクスチャを `map` と `bumpMap` に使う
- 人物と探査車は `heightAt` の中央差分から地表法線を求め、前後・左右の両方向で斜面へ姿勢を合わせる
- 探査車の6輪は各接地点の高さを個別にサンプリングし、サスペンション量を更新する
- 接地影は地面の法線へ沿い、ジャンプ中は高度に応じて薄く広がる
- 地表重力は落下加速度だけでなく、接地時のトラクション、空中制御、跳躍高度、滞空時間へ反映する
- 低重力では加減速が緩く、人物の跳躍は高く長い。高重力では短く重い動きになる
- 重力比は地球=1.00、月=0.17、火星=0.38。跳躍は単純な反比例ではなく宇宙服・探査車の出力限界を加味する
- 跳躍高は `base × (地球G/現在G)^0.62`（徒歩 base 0.60m / 探査車 0.42m、上限 4.7倍）。指数は 0.38 → **0.62**（2026-09-22）。星ごとの差が体感できないという指摘への対応で、実測 0.48m（地球）→1.73m（月）→2.11m（エウロパ）→2.75m（ミランダ）
- 落下だけ重力を **1.4倍**にする。上昇は実重力のまま浮遊感を残し、低重力での滞空が間延びしないようにする
- その星で最初に跳んだ1回だけ、`0.17 G / 跳躍高 1.7m / 地球の約2.9倍` をトーストで出す（差を数字でも伝える）

## 5-3b. 接地は描画メッシュに一致させる（2026-09-23 新設）

- 地形メッシュは 480 を 144 分割（1マス 3.33）した三角形。解析値 `heightAt()` とメッシュ面はマス内でずれ、実測で最大 **+2.13 / −1.33**。凹側で人物の脚が地面へ埋まっていた
- `buildTerrain()` が頂点高さを `TERMESH` に保存し、`meshHeightAt()` が PlaneGeometry と同じ三角形分割（対角は (ix,iz+1)-(ix+1,iz)）で補間する
- 接地・接地影・姿勢の法線・岩の据付・発見物と着陸船の据付・カメラの下限は、すべて `meshHeightAt()` を見る。`heightAt()` はメッシュ生成と岩・地形の分布にだけ使う
- 合格条件: 9天体で歩行260フレーム、足元と描画面の差が沈み・浮きともに 0.00

## 5-5. 星ごとの地表環境（2026-09-23 新設）

| 天体 | 地形 | 環境演出 | 温度表示 |
|---|---|---|---|
| 地球 | `port`（平ら） | 温暖化が進んだ想定。空は橙褐色、地面は乾いた黄土、熱の霞が地面から立ちのぼる | +48 ℃ |
| 金星 | `cloud`＋地溝3本（起伏 −31.4〜+7.9） | 落雷（稲妻メッシュ＋閃光＋距離比例で遅れる雷鳴）と常時の地鳴り。落雷直後は揺れが増す | +464 ℃ |
| エウロパ | `icefield`（専用。氷原＋リネア4本、起伏 −7.4〜+3.4、艶 roughness 0.42） | 水柱の噴出7本（到達高 38.4）。1本目は「氷下海のプルーム」の脇に置く | −171 ℃ |
| トリトン | `ice` | 窒素プルーム4本（到達高 25.4）。暗色・通常合成で氷原の上でも見え、風下へ 46 の暗い筋を引く | −235 ℃ |
| 月 / 水星 / 火星 / タイタン / ミランダ | 従来どおり | ― | +110 / −173 / −63 / −179 / −187 ℃ |

- 演出は SURF 設定のフラグ（`storm` / `heat` / `geyser`）で有効化し、`updGeysers` `updWeather` `updHaze` を `updSurface` から1本で更新する
- 地表HUDは 天体 → 重力 → **温度** → 発見 の順。温度は地表モードのみ表示する

## 5-7. 海王星の大暗斑（2026-09-23 改修）

- 大暗斑はテクスチャの楕円スタンプとして以前から存在したが、`rl 0.30 / rt 0.10 / 色(22,38,100)` と小さく淡く、青い帯に埋もれて画面上ほぼ判別できなかった
- `rl 0.50 / rt 0.185 / 色(14,27,82) 核(6,12,50)` へ拡大・濃色化。周囲は rgb(66,102,207) なので輝度比で約 7 倍の差が出る
- 渦に寄り添う明るいメタンの巻雲2本、「スクーター」、南半球の小暗斑 D2 を追加する
- 観測目標「大暗斑」は、従来の機械的な配置（`spherePos(..., i*2.39)`）をやめ、**テクスチャ上の斑の真上**（`SPOT_ANCHOR`）へ貼り付ける。自転するので位置は毎フレーム作り直す
  - 半径は惑星半径の **1.45倍**。1.28倍だと接近時に「高度が低い」警告（R*1.12）に触れる
  - 斑が裏側にある間はマーカーも惑星に隠れる（深度テストあり）。回ってくるのを待つ／回り込むのが手順になる
- 合格条件: 周回軌道でマーカーへ寄ると `neptune:dark` が記録され、画面に大暗斑が黒い渦として写っている

## 5-6. 空にしかない記録（2026-09-23 改修）

- 対象: 月「地球の出」/ 水星「巨大な太陽」/ 火星「青い薄明」/ エウロパ「木星の出」/ タイタン「見上げた環」
- 成立条件: 的が画面内（|NDC| ≤ 0.92）で撮影する。判定は変更なし
- 到達手段: ［空を見る］で仰角を振り切り、旋回で方角を合わせる。ドラッグでも可（下方向で見上げる）
- 誘導: 的が画面内なら「◎ ○○ ── いま撮れる」、画面外なら縁に方向矢印、**背後なら必ず横向きの矢印＋「は背後」**。以前は背後で表示ごと消えていた
- 合格条件: 5件すべて、ボタン→旋回→撮影のみで記録が成立する

## 5-4. 岩の当たり判定（2026-09-22 新設）

- 岩は生成時に台帳 `G.rocks`（中心・水平半径 `rx`・垂直半径 `ry`）へ登録し、**楕円体**で近似する
- 足元から `climb`（徒歩 1.25m / 探査車 1.15m）以内の高さなら天面を地面として扱い、乗り越える
- それより高い面は縁へ押し戻し、法線方向の速度だけを殺す。結果として緩斜面は登れ、切り立った面は壁になる
- 接地影・姿勢の法線・探査車のサスペンションは、地形ではなく「地形＋乗れる岩」の合成面 `contactHeight` を見る
- 発見物の半径 6.5m と着陸船の半径 8m からは岩を除去する。当たり判定が付いたことで記録地点を塞ぐ事故を防ぐ

## 6. 音声
- WebAudio。開始タップおよび最初の pointerdown で unlock
- 環境音ループ。ミュート時は出力オフ。状態は `tg.250.mute`

### 6-2. BGM（2026-09-23 実装）

- WebAudio とは**別系統**の HTMLAudioElement。AudioContext を作れない端末でも BGM は鳴る
- 要素は**2本**を使い回し、A/B でクロスフェードする（フェードアウト 0.8 秒／フェードイン 1.2 秒）
- iOS の解錠は「要素ごと」。開始タップの中で 2 本とも無音 WAV（8kHz/8bit/0.1秒の data URI）で `play()` を通し、以後は実ファイルを `src` に差し替えて鳴らす
  - 長さ0の WAV では WebKit が `error` を出して解錠できない。実測で `_dead=true` になったため、正しいヘッダの無音を使う
  - 4本すべてを実ファイルで先読みすると初回タップで **5.7MB** 走る。必要な曲だけ落とす構成にして、起動時の通信を **2.18MB** に抑えた
- `loop = true`。`playsinline` / `webkit-playsinline` 付き
- 曲の割り当て

| モード | ファイル |
|---|---|
| タイトル・星図 | `Beyond_the_Last_Horizon.m4a` |
| 航行 | `Beyond_the_Gravity_Well.m4a` |
| 周回軌道 | `Drifting_Past_The_Great_Divide.m4a` |
| 地表 | `Beneath_the_Silver_Pull.m4a` |

- ミュートは `Audio2.on` に追従（キー `tg.250.mute` を共有）。ポーズ・バックグラウンドで停止し、復帰で再開
- 合格条件: Chromium と WebKit の両方で、タップ前は無音・タップで再生・モード切替でクロスフェード・ミュート・ポーズ復帰が成立する

## 7. 保存
- `hoshimeguri.v1`: 記録と写真（data URL）
- `hoshimeguri.v1` 内の `solar:complete`: 太陽系コンプリート済みフラグ
- `tg.250.mute`: ミュート

## 8. ファイル構成
```text
250-hoshimeguri-kokai-nisshi/
  index.html
  fc-ios-controller.js
  fc-ios-controller.css
  three.min.js
  SPEC.md
  LEARNINGS.md
```

## 9. 実装制約
- 公開実体 20MB 以下
- 外部 CDN 禁止（フォントもシステムフォント）
- iOS: viewport / safe-area / ダブルタップ防止 / 明示ポーズ

## 10. テスト項目
- タイトルが出る
- 9天体の周回（`enterOrbit`）と7天体への着陸（`startDescend`→`surface`）でコンソールエラー0件
- 出港 → 星図で10天体がすべてタップできる → 出発で航行が始まる
- 操作盤 25%・ボタン 48px 以上
- harness PASS
- 素材0 / 6 / 14 / 24で、対応する航路だけが解放される
- 全82項目を満たすと `solar:complete` が保存され、エンディングが表示される
- 合計サイズ 20MB 以下

## 11. 未確定事項
- なし（公開ブロッカーなし）
