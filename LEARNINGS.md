# 250-hoshimeguri-kokai-nisshi 学び

## 2026-09-23（追補2）視点復帰の本当の原因・構造物の当たり判定・ダッシュ

- **「直したのに直らない」の2回目。今度は状態の二重管理だった。** ［視点を戻す］は自分の環境では動いていた（実UIタップで 0.95→0.13 を3環境で確認）。それでも直らない報告が来た原因は、**ドラッグで視点を動かすと `G.skyGaze` が false に落ちる**こと。見上げたままなのにボタンが［空を見る］に戻り、押しても下がらない（=もう一度上げるだけ）状態になる。**「いま見上げているか」は専用フラグではなく、実際の `IN.look.y` から判定する**ように変えて解消した
  - 教訓: 画面の見た目を決める値（仰角）と、UIの文言を決める値（フラグ）が別々にあると必ずズレる。**UIは状態そのものから導出する**
  - 戻す経路を3つ用意した（ボタン・撮影成立・歩き出し）。1つしか無い脱出口は塞がる
- **`pointerup` のあとに `lostpointercapture` も飛ぶ。** Bボタンを長押し→解放したとき、`end` が2回走り、2回目は `dashing` が既に false なので**撮影が発火していた**（実測 photos 1→2）。1押下1回のガードを入れて解消。`setPointerCapture` を使う長押し判定では必ず起きる
- **見た目のメッシュは、置いた時点で「触れる」と期待される（2回目）。** 9/22 に岩へ判定を付けたが、**発見物の構造物と着陸船は対象外のまま**だった。塔もドームも中央丘も通り抜けていた。種類ごとの近似殻を岩と同じ台帳へ積んだ
  - 積む順序が落とし穴。`clearZones`（発見物の周りから岩を退ける処理）より**先に積むと自分の殻が消える**
  - 殻を足すと**自分の記録に近づけなくなる**。発見の成立距離を殻の大きさぶん広げる処理とセットで入れる必要がある（9/22 と同じ「判定を足すと到達性が壊れる」構図）
- ダッシュの実測倍率は 3 倍にならない（徒歩 2.63 / 探査車 2.95）。加速が `lerp` なので3秒の移動距離では最高速に届かないため。**倍率は最高速で定義し、距離で検算しない**

### 実測（Chromium デスクトップ／WebKit iPhone相当390px の2環境で同一結果）

| 項目 | 実測 |
|---|---|
| ボタンで上げる→戻す | 0.95 → **0.13** |
| **ドラッグで上げる→［視点を戻す］** | 0.95・ボタン表示あり → **0.13**（修正前はここが戻らなかった） |
| 撮影で記録成立→自動復帰 | **0.13**・ボタンも消える |
| 歩き出し→自動復帰 | **0.13** |
| 構造物のめり込み | 5天体の全発見物へ8方向から全力衝突。最大 **0.24**（水星のドーム。視覚半径4.2に対し停止 4.16＝ほぼ接触） |
| 記録の到達性 | 月5/5・水星4/4・地球5/5・エウロパ3/3・火星5/5 |
| ダッシュ | 徒歩 19.1→**50.2**（2.63倍）／探査車 46.7→**137.9**（2.95倍） |
| Bボタン | 短押し=写真1枚・dash 0 ／ 長押し中=dash 1・ラベル「走る」・`.dashing` 付与 ／ 解放後=dash 0・**写真は増えない** |
| コンソールエラー | 0件（両エンジン） |

## 2026-09-23（追補）大暗斑・視点の復帰・BGM

- **「テクスチャに焼いてある」と「画面で見える」は別。** 海王星の大暗斑はオーバルとして前から存在し、テクスチャを直接サンプリングすると斑 rgb(6,12,50) / 周囲 rgb(66,102,207) と 7 倍の輝度差があった。それでも遊んでいて気づけないのは、円盤上の占有が小さく（rl 0.30／rt 0.10）、いつも縁の近くに来ていたから。**判定できる差**と**気づける差**を分けて測る
- 3D の「見えない」は、まず**構図**を疑う。最初の2回は自分のプローブのカメラ方角が 180°／90° ずれていて「描かれていない」と誤読しかけた。レイキャストで画面中心の UV を実測し、`自転角 R に対し 中心経度 = 5.78 - R` を6点で確認してから結論を出した。**幾何の推論より、1回の実測**
- **観測目標が地物と無関係な場所に浮いていた。** 目標は `spherePos(r, sin(i*1.7)*0.72, i*2.39)` の機械的配置で、大暗斑を「観測する」のに斑を見る必要がなかった。テクスチャ座標へ貼り付ける `SPOT_ANCHOR` を入れ、自転に追従させた。**記録の名前と、画面に写っているものを一致させる**
- **トグルは「戻す側」を書き忘れる。** ［空を見る］は仰角を 0.95 へ上げる処理しか無く、解除しても上を向いたままだった。さらにボタンの表示条件が `if(G.skyShot)` だったため、**撮影して記録が成立した瞬間にボタンが消え、見上げたまま戻せなくなる**。①解除で 0.13 へ戻す ②記録成立時に自動で戻す ③見上げている間はボタンを出し続ける、の3つをセットで入れた
- **iOS の音声解錠は「要素ごと」。** 4曲を実ファイルで先読みして解錠すると、初回タップで 5.7MB 走る。要素2本を無音 WAV で解錠してから `src` を差し替える方式にして、起動時の通信を 2.18MB に落とした。差し替え後も再生できることは WebKit で実測している
- **無音ファイルは「正しい無音」でないと解錠に使えない。** 長さ0の WAV（`data` チャンク 0 バイト）を使ったところ WebKit が `error` を発火し、要素が `_dead=true` になっていた。8kHz/8bit/0.1秒の正規の無音へ差し替えて解消
- **`hidden` 属性は CSS に負ける。** 温度行を `<div class="row" hidden>` で足したが、`#navBox .row{display:flex}` が UA の `[hidden]{display:none}` に勝ち、航行・周回でも「温度 —」が出ていた。`#navBox .row[hidden]{display:none}` を明示して解決

### 実測

| 項目 | 実測 |
|---|---|
| 大暗斑の視認 | テクスチャ輝度 斑 14.5 / 周囲 103.2。緯度 −0.38 の行で最暗点は経度 1.69（＝設計値 1.70） |
| 大暗斑の記録 | 周回軌道でマーカーへ寄せる自動操作 900 フレームで `neptune:dark` 記録成立、観測 1/3、素材トースト表示 |
| 視点の復帰 | 0.13 →［空を見る］→ 0.95 →［視点を戻す］→ 0.13。撮影成立時は自動で 0.13 へ戻りボタンも消える。手動ドラッグ中は 0.946 を保持（勝手に戻らない） |
| BGM | Chromium / WebKit 両方で、タップ前 無音 → タップで再生 → 地表・周回でクロスフェード → ミュート全要素反映 → ポーズ停止・復帰再開。解錠要素の `_dead` はどちらも false |
| 通信 | 起動時 **2.18MB**（旧方式は 4曲先読みで 6.27MB）。フォルダ合計 6.31MB／20MB |
| 監査 | ×5 → **×3**（BGMループ ○、未参照アセット 0件。残りは z-index 重複・console.log・未コミット差分） |

harness **PASS**: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-23T02-04-29-148Z.md`

### 未検証

- **実機 iPhone での BGM は未確認。** WebKit は iOS Safari と同じエンジンだが、実機はジェスチャー要件と同時再生チャンネル数がさらに厳しい。`/game-local-test`（Xcodeシミュレータ）か実機での確認が必要

## 2026-09-23 めり込み・空の記録・星ごとの地表環境

- **「地形の高さ関数」と「描かれている地形」は別物だった。** 接地は解析値 `heightAt()` を使い、画面の地面は 480 を 144 分割（1マス 3.33）した三角形メッシュ。マスの内側では両者がずれ、実測で **最大 +2.13 ／ −1.33** の差があった。凹側では脚（長さ 1.0）がまるごと埋まる。描画メッシュの頂点を `TERMESH` に保存し、`meshHeightAt()` で PlaneGeometry と同じ三角形分割（対角は (ix,iz+1)-(ix+1,iz)）を再現して接地・影・岩・設置物・カメラをすべてそちらへ付け替えた
- 高さ関数を増やすと**構築順が落とし穴になる**。`buildTerrain()` の頂点色計算は法線を引くので、先頭で `TERMESH=null` にして前の星のグリッドを読ませない。グリッドの確定は頂点を書き終えた後
- **「直したのに直っていない」は、判定ではなく到達手段が塞がっていることがある。** 9/23 午前にエウロパ・タイタンの4つ目（空の記録）の判定を「カメラの向き」から「画面に写っているか」へ直したが、まだ取れなかった。実測すると判定は通る（NDC 0.18〜0.29 で画面内）。塞いでいたのは入力側の2点 ──
  - 仰角 `IN.look.y` を上げる手段が**ポインタドラッグだけ**で、しかも**下へドラッグすると見上げる**（`+= dy*0.0035`）。ヒントは「視点を上へ振り切り」と書いてあり、指示どおりに動かすと逆を向く
  - FCコントローラは十字が前後・旋回のみで、**iPhoneでは仰角を変える手段が存在しない**＝物理的に取得不能だった
  → 行動ボタンに **［空を見る］（Q）** を追加し、押している間は仰角を 0.95 へ自動で振り切る。左右（旋回）だけで的を入れられるので、実機のコントローラでも完結する
- 誘導表示は**一番必要なときに消えていた**。`project()` は的がカメラの背後にあると符号が反転するため、旧実装は背後で `null` を返してラベルごと隠していた。カメラ座標から向きだけを作り、背後なら必ず横向きの矢印＋「は背後」を出す
- **地表の演出は SURF 側のフラグで持たせると、星ごとの個性が1行で足せる。** `storm` / `heat` / `geyser` を SURF に置き、`updGeysers` `updWeather` `updHaze` を `updSurface` から1本で回す形にした
- 明るい地表の上では**加算合成の噴煙は消える**。トリトンの窒素プルームは加算だと氷原に溶けて見えなかったので、暗色＋通常合成に変え、風下へ伸びる暗い堆積跡（地面の起伏に沿わせた板）を足した。エウロパの水柱は逆に加算のままで映える

### 今回の変更（実測付き）

| 変更 | 実測 |
|---|---|
| 接地を描画メッシュ一致に | 9天体で歩行260フレーム、足元と描画面の差 **沈み 0.0000 / 浮き 0.0000**（改修前は月 −0.892・エウロパ −1.325） |
| ［空を見る］＋誘導矢印 | 月/水星/火星/エウロパ/タイタンの5件すべてで、ボタン→旋回→撮影のみで記録成立。画面内 NDC は最良 0.133〜0.291。背後では「◀ 木星 は背後」を表示 |
| 金星：谷・雷・地鳴り | 起伏 **−31.4 〜 +7.9**（改修前は ±3.4 相当の平地）。谷の縁 −8.1 から前進のみで −13.6 まで降下＝歩いて降りられる。落雷は稲妻メッシュ＋閃光＋距離比例の遅延雷鳴、揺れ量 0.2 |
| 地球：温暖化 | 空を青→橙褐色、地面を緑→乾いた黄土、霞を fogFar 430 に短縮、熱の霞220粒。温度 **+48 ℃** |
| エウロパ：氷の大地＋噴出 | 専用地形 `icefield`（起伏 −7.4〜+3.4 の氷原＋リネア4本）、艶（roughness 0.42）、水柱7本・到達高 **38.4** |
| トリトン：わずかな火山活動 | 窒素プルーム4本・到達高 **25.4**、風下へ 46 の暗い筋 |
| 地表HUDに温度 | 重力の直下に9天体ぶん（地球 +48 / 月 +110 / 水星 −173 / 金星 +464 / 火星 −63 / エウロパ −171 / タイタン −179 / ミランダ −187 / トリトン −235 ℃） |

コンソールエラー 0件。game harness **PASS**: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-23T01-17-34-835Z.md`（0.76MB）

### 未処理（タイタンの判断待ち）

- `Beneath_the_Silver_Pull.m4a` ほか3本（計 5.7MB）が **index.html から参照されていない**。BGM実装の途中で置かれたもの。今回のコミットには含めていない（未参照アセットは人間箱）

## 2026-09-22 星ごとの跳躍差・岩の貫通・船体の段階可視化

- **前回の調整（跳躍指数 0.38）が今回の不満の直接の原因だった。** 「誇張されすぎ」と判断して月2.17m→1.09mへ抑えた結果、地球0.55m〜ミランダ1.58mの2.9倍幅しか残らず、星を移っても同じ跳び方に感じる状態になった。指数を0.62へ戻し、上限も1.58m→2.82mへ広げた
- 滞空の間延びは「跳躍高を削る」以外でも解ける。**上昇は実重力のまま、下降だけ1.4倍**にすると、浮遊感（＝低重力の手触り）を残したまま着地待ちの退屈だけが減る。高さと滞空を1つのパラメータで同時に決めていたのが前回の設計ミス
- 体感差は数字でも言う。その星で最初に跳んだ1回だけ「0.17 G / 跳躍高 1.7m / 地球の約2.9倍」をトーストで出した。プレイヤーは2つの星を続けて跳び比べられないので、比較の基準は画面が提供する必要がある
- **見た目だけのオブジェクトは、置いた時点で「触れる」と期待される。** 岩110個は装飾として置かれ当たり判定が無く、通り抜けていた。楕円体近似の台帳 `G.rocks` を持たせ、足元+1.25m以内の高さなら天面に乗り、それ以上は縁へ押し返す方式にした。緩斜面は登れて切り立った面は壁になる
- 当たり判定を足すと**到達性が壊れる副作用**が出る。発見物や着陸船の上に岩が乗ると記録できなくなるため、発見物半径6.5m・着陸船半径8mから岩を除去する処理を同時に入れた。判定を足す改修は「入れない場所」の定義とセットにする
- 強化条件は実装済みでも、画面に出ていなければ存在しないのと同じ。HULL I〜IVの素材閾値はコードにあったが、表示は `船体 I / 素材 0 → 6` だけで、**何をすると増えるのか・上がると何が起きるのかが書かれていなかった**。「観測・記録をあと6件で HULL II（火星の航路が開く）」へ書き換えた
- 進行の手応えは文字より形で出る。`buildShip(tier)` を段階制にし、I 双発 → II 放熱フィン＋4発 → III 通信皿・カナード → IV 衝撃盾リング＋5発、色は白銀→金へ推移させた（メッシュ数 20→28→34→41）
- 機首のコーンは旧実装で `rotation.x = -Math.PI/2`、つまり**尖端が後ろを向いていた**（前が漏斗形）。+π/2 へ直して観測ブームを足すだけで、同じ部品数でも輪郭が「速そう」になる

### 実測（Chromium 390×844・実ゲームループで計測）

- 跳躍高 実測: 地球 0.48m / 火星 0.67m（探査車） / 月 1.73m / エウロパ 2.11m / ミランダ 2.75m。改修前は地球0.55m〜ミランダ1.58m
- 岩: 最も高い岩（rx 2.80 / ry 3.31）へ9m/sで正面衝突 → めり込みフレーム **0**、天面に乗って停止。低い岩（ry 0.40）は乗り越え通過を確認
- 船体: `buildShip(0..3)` のメッシュ数 20 / 28 / 34 / 41、エンジン炎 2 / 4 / 4 / 5
- 星図表示: HULL I で「観測・記録をあと6件で HULL II（火星の航路が開く）」、HULL IV で「最終船体 ─ 全航路が開いている」。390px幅で溢れなし
- コンソールエラー 0件。game harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-22T11-53-27-126Z.md`（0.74MB）
- **未検証**: iPhone実機でのタップ操作感、岩に挟まって動けなくなる状況が長時間プレイで起きないか

## 2026-09-22 帰還導線・ファミコン操作・低重力の再監査

- 操作盤の見た目がファミコンでも、固定ラベルと実際の役割が状態ごとに食い違うと操作は覚えられない。SELECTを「戻る系」に固定し、地表=船へ帰還、航行/周回=星図へ戻る、とした
- 地表のAは、発見物の近くでは記録、対象がなければジャンプ。A/B/SELECTの小ラベルも現在の状態に同期させた
- 旧実装の重力比（月0.17G、火星0.38G）は正しい。一方、跳躍高へ強い指数を掛けていたため、月2.17m・滞空2.24秒まで誇張されていた。宇宙服と探査車の出力限界を加味し、人物は月約1.09m、火星約0.79mへ抑えた
- 小学生が退屈するという判断は実ユーザーテストではなくヒューリスティック評価。主因候補は、82項目の長い収集、失敗/危機/競争の不在、発見物が26m以内まで案内されない盲目的移動、記録後の遊びの変化が素材閾値まで遅いこと

### 実測

- Chromium 390×844: 操作盤25%、全ボタン48px以上、月面でAジャンプ、SELECT帰還後に `MODE=orbit` とラベル「星図」を確認
- 重力表示: 月0.17G / 火星0.38G。調整後の目標跳躍高: 月1.09 / 火星0.79
- game harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-22T01-18-33-055Z.md`（62 RAF/秒、0.73MB、コンソールエラー0件）
- 小学生本人によるプレイテストとiPhone実機確認は未実施

## 2026-09-22 探索素材・船体強化・パンキッシュエンディング

- 日誌の項目を別の通貨として二重保存せず、既存の観測・発見キーから素材所持数を導出した。既存セーブを移行せず、記録と素材の正本が割れない
- 航路解放を「素材0 / 6 / 14 / 24」の4段階にした。最初から内惑星5天体へ行けるので詰まず、各段階の解放済み天体だけで次の必要数を集められる
- 遠方天体は消さず、星図に `LOCKED` と必要素材数を残した。「次にどこへ行けるか」が目的として見える
- エンディングは既存の琥珀色・静謐な日誌調を続けず、赤・シアン・黒、走査線、グリッチ、傾いた版面へ切り替えた。長い収集の後に画面文法そのものが壊れることで終幕を強くする
- 初稿の「太陽系は檻だった」は閉塞と脅威が結論になっていた。タイタンの言葉「太陽系の先はやはり広かった。広大で壮大で、雄大だった」を受け、終幕を解放と畏敬へ修正
- 謎は説明し切らず、「まだ名前のない星」「星間を横切る説明できない何か」「十一番目の座標」の3点を残した。恐怖ではなく、次の航海を呼ぶ未知として扱う

### 実測

- 素材0: HULL I、太陽〜月の5天体を解放
- 素材6: HULL II、火星を解放
- 素材14: HULL III、木星・土星を解放
- 素材24: HULL IV、天王星・海王星を解放
- 全82 / 82項目で `solar:complete=true`、HULL IV、エンディング表示を確認
- iPhone幅390×844で星図とエンディングを画像確認。コンソールエラー0件
- game harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-22T00-05-46-987Z.md`（49 RAF/秒、0.73MB、操作盤25.0%、ボタン適合）
- iPhone実機は未実施

## 2026-09-22 地形追従と重力別の地表移動

- `heightAt` を見た目の地形生成だけで終わらせず、有限差分で `groundNormalAt` を作り、人物・探査車・接地影の姿勢にも同じ高さ場を使った。左右だけの傾きより、クレーターの縁や進行方向の坂が動きに現れる
- 探査車は車体を法線へ合わせるだけでは凹凸感が足りない。6輪それぞれのワールド座標で高さを取り、ローカルYを独立補間すると、窪みをまたぐ様子が見える
- ジャンプ初速を固定値＋重力にすると天体差が曖昧になる。目標跳躍高を重力比から決め、`sqrt(2gh)` で初速へ戻すと、同じ入力でも月は長く浮き、地球は短く落ちる
- 地面は高さだけの頂点色から、斜度による暗部・細粒テクスチャ・バンプへ変更。遠景の輪郭と足元の材質感を別のスケールで作る

### 実測

- ブラウザ内120Hz物理検証: 月の滞空 2.24秒・最高高度 2.17、地球の滞空 0.53秒・最高高度 0.70
- 火星の斜面で6輪Yが個別値になり、車体上方向 `[-0.194, 0.979, 0.067]` が地表法線 `[-0.188, 0.977, 0.098]` に追従
- 月面・火星地表のブラウザ描画でコンソールエラー0件
- game harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-21T23-51-12-829Z.md`（58 RAF/秒、0.72MB、操作盤25.0%、ボタン適合）
- iPhone実機は未実施

## 2026-09-20 フォルダに作品名を付けた

`250-day074` は日付の仮名で、中身が「星めぐり航海日誌」だとフォルダ一覧から読めない。番号250は維持し、`250-hoshimeguri-kokai-nisshi` にした。

- 本体は `index.html` のまま
- リポジトリ横断の参照は `250-day074` ゼロ件だったので、置換対象はフォルダ名のみ
- 親リポジトリでは未追跡（`??`）のまま

## 2026-09-20 25%ファミコン風操作盤

- `titans-ui` の FC iOS Controller をゲームフォルダへコピー（Pages単体公開のため）。`#game-shell` / `#game-stage` / `#screen-wrap` / `#control-deck` の 75/25。
- 十字＝移動、A＝記録/着陸、B＝撮影、SELECT＝加速、START＝ポーズ。ミュートはポーズ内と `m` キー。保存キーは `tg.250.mute`（航海記録 `hoshimeguri.v1` は維持）。
- SELECT/START を 48px 以上に拡げ、操作盤の `padding-bottom` を 8px 以上にした。公式CSSのままではハーネスのボタン下限と余白下限を割る。
- Pointer Events + `setPointerCapture`。描画先は `#screen-wrap`。
- harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-20T04-26-39-652Z.md`（操作盤 25.0%、非重複、ボタン適合、RAF 36）。十字が画面端で欠けないよう操作盤内サイズを下げた。iPhone実機は未実施。

## 2026-09-20 星空を3枚重ねにした

- 点群（最大3000）をやめ、遠／中／近の星空キャンバス3枚を `#star-stack` に重ねた。WebGLは `alpha:true` でその上に惑星だけ描く。
- 視点の回転で3枚をずらす。手前の層だけ明るさを揺らす。大気のある地表では重ねを隠す。
- 3枚をWebGLのスフィアにすると全面加算が3回走り、ハーネスが 22 RAF/秒で落ちた。DOM重ねに変えて 33 RAF/秒で PASS。
- harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-20T04-32-33-368Z.md`。iPhone実機は未実施。

## 2026-09-20 オフライン起動と公開

- Three.js を CDN から読んでいたため、通信が切れると「機関始動…」のまま止まる。`244` と同じ r128 を `three.min.js` として同梱し、起動はローカル読み込みだけにした。
- Google Fonts も外し、Hiragino 系のシステムフォントにした。外部URL参照はゼロ。
- タイトル／星図の主要ボタンは `pointerdown`。日誌写真は `data:image/` だけ表示し、名前はエスケープする。
- `#loading{display:flex}` が `hidden` 属性の `display:none` を上書きし、起動後も「機関始動…」が被さっていた。`#loading[hidden]` を `display:none!important` にした。
- ハーネスの描画ループが負荷で 28 RAF/秒になることがある。WebGL の antialias を切り、pixelRatio 上限を 1.5 にした（撮影用の preserveDrawingBuffer は維持）。
- harness PASS: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-20T05-38-08-446Z.md`（60 RAF/秒）。iPhone 16 シミュレータの Safari でタイトル画面（地球・出港する）を確認。実機は未実施。
- 公開ブロッカーになる未確定事項は SPEC に無し。

## 2026-09-20 操作性改修と iOS 公開

- 十字の左右が地表で「平行移動＋視点回転」を同時に踏んで滑っていた。地表は左右＝視点旋回のみにした。
- 航行・周回は画面ドラッグを旋回に使う。チェイスカメラが `IN.look` を捨てていたので、スマホで空をなぞっても曲がらなかった。
- 自動補正が `IN.side` だけを見ていて、旋回入力中も針路を戻していた。`IN.turn` とドラッグ量を見るようにした。
- HUD・星図・ポーズが `click` のままだったので `pointerdown` + `setPointerCapture` に揃えた。スマホ幅で消していた操作ヒントを復活。
- SELECT/START の 5px 表記をやめて「加速／停止」。A/B に「記録／撮影」。十字の連続バイブを方向変化時だけにした。
- ブラウザで出港→月／火星／地球を3回通した。ハーネス PASS 4回: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-20T10-37-41-717Z.md` / `...T10-37-50-375Z.md` / `...T10-37-53-208Z.md` / `...T10-41-17-088Z.md`（62 RAF/秒・操作盤25.0%・ボタン適合）。iPhone 16 シミュレータの Safari でタイトル＋操作盤（加速／停止／記録／撮影）を確認。実機は未実施。

## 2026-09-21 星の表面を作り直した（UVの絵 → 球面ノイズ＋凹凸＋縁光り）

タイタン指示「星の表面をもっとリアルに」。周回・航行で見える**天体の外側**を作り直した（着陸後の地表メッシュは今回さわっていない）。

### やったこと

- **球面座標で3Dノイズを引く**。旧実装は512×256のUV画像に `fillRect` で描いていたため、極で模様が一点に潰れ、赤道で横に伸びていた（＝地図を貼った球）。768×384に上げ、画素ごとに緯度経度→3D座標を作ってノイズを引く
- **オクターブごとに座標を回す**。回さないと値ノイズの格子が縦横の筋として残り、太陽の粒状斑が方眼紙に見えた
- **凹凸マップ（bumpMap）を岩と氷に追加**。ただし高度場を「大地形（色用）」と「細部（凹凸用）」に分けるまでは、クレーターが大地形の階調に埋もれて出なかった
- 種別ごとの作り分け: 月の海／火星の明暗と極冠／水星の高密度クレーター／エウロパのリネア／木星の帯と大赤斑／金星のダークY／太陽の粒状斑と黒点群
- **大気はフレネルの縁光り**に変更（旧: 一定の濃さで加算＝風船を青く塗った状態）。`rim*(1-rim^5)` で殻の輪郭線を消す。大気のない月・水星には付けない（岩のふちが甘くなる）
- **太陽は周縁減光**をシェーダーで入れた。均一に塗ると平たい円盤に見える

### つまずきと実測

- `#include <tonemapping_pars_fragment>` を自前シェーダーに書いたら `'LinearToneMapping' : function already has a body`。**three r128 は ShaderMaterial にも pars を自動で注入する**。必要なのは末尾の `tonemapping_fragment` / `encodings_fragment` だけ
- **アルベドに `sRGBEncoding` を指定しないと全部が白っぽく浮く**。three既定は Linear なので、手で塗った色がリニア値として扱われ、出力時にもう一度 sRGB へ変換されて持ち上がる。木星の帯が灰色っぽかったのはこれ。環のテクスチャも同じ理由で灰色に濁っていた
- 淡い基準色（土星 `#e2cd9b`・金星）は明度を倍率で持ち上げると白飛びする。**ゾーンの明るさは絶対値で頭打ち**にした（`min(1.24, 0.80/基準明度)`）
- 直射 1.55 は正面が飛ぶので 1.15 へ（ACESの圧縮任せにしない）

### 描画負荷（harnessが 60→26 RAF/秒 に落ちた件）

同一セッション内で要素を外しながら測ったら犯人が分かった。**推測で削っても外れる**ので、この測り方が要る（実行ごとの比較はマシン負荷で埋もれる）。

| 外したもの | RAF/秒 |
|---|---:|
| なし（基準） | 31 |
| 大気シェル | 35 |
| bumpMap | 31（変わらず） |
| **specularMap** | **49** |

- 海だけ光らせる `specularMap` が最も重かった → マップをやめ、材質側の一律 `specular` にした（見た目はほぼ同じ）
- 雲も別の球に貼ると惑星の面積ぶん描画が1枚増えるので、本体テクスチャへ焼き込んだ
- `MeshStandardMaterial` → `MeshPhongMaterial`。惑星は画面占有が大きく、物理BRDFの1画素あたりが効く
- 地球には bumpMap を付けない（惑星規模では山の起伏は見えないのに、画面占有は最大）

### 証跡

- harness PASS 3連: `docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-21T00-37-13-269Z.md` / `...T00-37-18-643Z.md` / `...T00-37-24-020Z.md`（36 / 40 / 35 RAF/秒・コンソールエラー0件・0.71MB）
- 9天体の周回と7天体の着陸（`descend`→`surface`）をヘッドレスで通し、コンソールエラー0件を確認
- ブラウザ実機（Chromium 390×844）でタイトル・月・火星・木星・土星・太陽の見た目を画像で確認。**iPhone実機は未実施**

## 2026-09-21 星図の行き先選びを直した

タイタン指示「星を選ぶ時のUIが良くない」。動線を端末サイズで実測してから直した。

### 実測で分かったこと

- **iPhone SE（320×568）と横向き（844×390）で、星の列の高さが 10px まで潰れて消えていた。** `#map` が縦フレックスなのに `.rail` が `flex-shrink:1` のまま、下の詳細パネルに押し潰されていた
- iPhone 14 縦（390×844）でもレール幅 668px に対し見える幅 354px。**木星〜海王星は画面外**
- 星ボタンが `pointerdown` + `setPointerCapture` + `preventDefault` なので、横スワイプがボタン上で奪われる。隙間がほぼ無く、外の星へスクロールできなかった
- 選択のたびに `innerHTML=''` でレールを作り直しており、スクロール位置も飛ぶ

### 直したこと

- 横の点列をやめ、**名前付きの2列ボタン**にした（広い／低い画面は5列）
- リストだけが縦スクロール。詳細と「出発」は星図の下に固定して、短い画面でも CTA が見える
- 日誌ボタンは見出し横へ移した（詳細内だと SE で出発と折り返して場所を食う）
- 横向きは詳細を1行（名前＋出発）にして、10天体が2段で全部見えるようにした
- リストは作り直さず状態だけ更新。タップはスクロールと区別する `onPtrTap`（capture しない）

### 検証（ブラウザ Chromium。iPhone実機は未実施）

| 画面 | 結果 |
|---|---|
| 390×844 | 10天体がすべて画面内。出発は stage 内。ボタン高さ 63px |
| 320×568 | ピッカーは残る（以前は 10px で消滅）。月が選択表示。出発は欠けない。リストは縦スクロール |
| 844×390 | 5列×2段で10天体すべて。出発は欠けない |
| 動線 | 出港 → 海王星を選ぶ → 日誌を開閉 → 海王星へ出発 → `MODE=cruise` `CUR=neptune` |
# 2026-09-22 iOS公開前検証

- SELECT/STARTのDOM領域が約59×23pxで48px基準を満たさなかった。疑似要素の拡張だけでは隣の操作と重なりやすいため、本体を48px高にし、細い外観を疑似要素へ分離した。
- 修正後の共通harnessは14項目PASS。証跡: `../docs/harness-reports/250-hoshimeguri-kokai-nisshi-2026-09-22T11-22-23-709Z.md`。
- WebKitで出港→星図10天体→航行→停止→消音保存→星図をタップ検証。AudioContext running、ページ例外0。
- 状態注入による検証: 10天体周回、9地表の降下→A操作→船へ帰還、素材0/6/14/24の航路解放、全項目達成→エンディング保存→再読込→星図を確認。自然プレイでの全項目収集は未検証。
- 320×568 / 375×667 / 390×844 / 430×932の縦画面で全操作ボタン48px以上・画面内を検証。iOS 18.4 / iPhone 16シミュレータではタイトル描画を目視確認。実機での音の聴取は未検証。
